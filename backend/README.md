# Backend - Malawi Ride-Hailing (FastAPI)

Quickstart:
1. Copy .env.example to .env and adjust values.
2. Build with docker-compose: docker-compose up --build
3. API available at http://localhost:8000
